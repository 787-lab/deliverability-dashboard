SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
RESEND_API_KEY=your-resend-api-key
RESEND_FROM_EMAIL=onboarding@resend.dev

# Only needed to run app/server.py (the on-demand "Check Now" HTTP endpoint).
# Shared with apps/web's CHECKER_API_SECRET — must match exactly.
CHECK_API_SECRET=generate-a-long-random-string
